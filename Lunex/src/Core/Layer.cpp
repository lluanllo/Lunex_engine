#include "stpch.h"
#include "Layer.h"

namespace Lunex {
	Layer::Layer(const std::string& name)
		: m_DebugName(name) {
	}
	
	Layer::~Layer() {
		// Destructor implementation can be added if needed
	}
}