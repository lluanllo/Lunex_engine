#pragma once

/**
 * @file TextureCompression.h
 * @brief Texture compression for the asset pipeline
 * 
 * AAA Architecture: TextureCompression lives in AssetPipeline/Texture/
 * Handles offline texture compression to GPU formats.
 */

// Re-export from original location for now
// TODO: Move implementation here after full migration
#include "Renderer/TextureCompression.h"
