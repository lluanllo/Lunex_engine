#pragma once

/**
 * @file KTXTextureLoader.h
 * @brief KTX texture loading for asset pipeline
 * 
 * AAA Architecture: KTXTextureLoader lives in AssetPipeline/Texture/
 * Handles loading compressed KTX2 textures at runtime.
 */

// Re-export from original location for now
#include "Renderer/KTXTextureLoader.h"
